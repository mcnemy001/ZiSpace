<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.ZiSpace" parent="Base.Theme.ZiSpace">
        <!-- Mengatur ikon splash screen menjadi transparan -->
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <!-- Mengatur latar belakang splash screen -->
        <item name="android:windowSplashScreenBackground">@color/background_1</item>
        <!-- Menonaktifkan animasi splash screen -->
        <item name="android:windowSplashScreenAnimationDuration">0</item>
    </style>
</resources>
